name: Testnet Preview

on:
  push:
    branches:
      - main
  pull_request:
    paths:
      - 'src/**'

jobs:
  testnet-preivew:
    uses: ./.github/workflows/deploy-testnet.yml
    with:
      deploy-account-address: ${{ env.DEPLOY_ACCOUNT }}
      signer-account-address: ${{ env.DEPLOY_ACCOUNT }}
      signer-public-key: ${{ env.SIGNER_PUBLIC_KEY }}
    secrets:
      SIGNER_PRIVATE_KEY: ${{ secrets.SIGNER_PRIVATE_KEY }}
